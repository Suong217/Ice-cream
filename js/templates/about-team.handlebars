<h3 class="text-dark">Our team</h3>
<div class="row">
    {{#each data}}
    <div class="col-md-3">
        <a href="#" onclick="loadData('team/{{id}}', '#content', 'about-details');">
            <img src="{{thumbpath}}" alt="" class="image-rounded">
        </a>
        <p class="text-dark font-weight-bold pt-3">{{name}}</p>
        <p>{{summary}}</p>
    </div>
    {{/each}}
</div>